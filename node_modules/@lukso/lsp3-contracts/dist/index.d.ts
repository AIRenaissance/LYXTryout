import { Verification } from '@lukso/lsp2-contracts';

type LSP3ProfileMetadataJSON = {
    LSP3Profile: LSP3ProfileMetadata;
};
type LSP3ProfileMetadata = {
    name: string;
    description: string;
    profileImage?: ImageMetadata[];
    backgroundImage?: ImageMetadata[];
    tags?: string[];
    links?: LinkMetadata[];
    avatar?: AssetMetadata[];
};
type ImageMetadata = {
    width: number;
    height: number;
    verification?: Verification;
    url: string;
};
type LinkMetadata = {
    title: string;
    url: string;
};
type AssetMetadata = {
    verification?: Verification;
    url: string;
    fileType: string;
};
declare const LSP3SupportedStandard: {
    key: string;
    value: string;
};
declare const LSP3DataKeys: {
    SupportedStandards_LSP3: string;
    LSP3Profile: string;
};

export { type AssetMetadata, type ImageMetadata, LSP3DataKeys, type LSP3ProfileMetadata, type LSP3ProfileMetadataJSON, LSP3SupportedStandard, type LinkMetadata };
